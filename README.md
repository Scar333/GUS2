1. module for auth
(
            page=browser.page,
            user_name=user_name,
            logger=main_logger,
            logger_path=main_logger_path
        + DATE FROM TO
        )

2. Check auth

BaseBrowserError - не смог найт кнопку


handle_critical_error - критичная ошибка все пиздец



https://ej.sudrf.ru/appeals/